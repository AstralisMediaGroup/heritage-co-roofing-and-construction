---
import HeritagePlaceholder from './HeritagePlaceholder.astro';
import { eastTexas, centralArkansas } from '../content/cities';
---

<div class="region-grid">
	<div>
		<p class="region-h">East Texas</p>
		<p class="region-cities">{eastTexas.join(' · ')}</p>
		<HeritagePlaceholder fact="Full East Texas city list (replaces the '+ 25 more cities' hedge from Build Pack §6) — see Founder Input page §9" />
	</div>
	<div>
		<p class="region-h">Central Arkansas</p>
		<p class="region-cities">{centralArkansas.join(' · ')}</p>
		<HeritagePlaceholder fact="Full Central Arkansas city list — see Founder Input page §9" />
	</div>
</div>

<style>
	.region-grid {
		display: grid; grid-template-columns: 1fr 1fr;
		gap: 56px; max-width: 800px; margin: 0 auto;
	}
	.region-h {
		color: #1a1a1a; font-size: 22px; font-weight: 600;
		margin: 0 0 16px; padding-bottom: 12px;
		border-bottom: 2px solid #1a1a1a; letter-spacing: -0.3px;
	}
	.region-cities { color: #5a5347; font-size: 15px; line-height: 1.85; margin-bottom: 16px; }
</style>
